# 🚀 دليل تشغيل المشروع - خطوة بخطوة

## 📥 كيفية الحصول على الكود

### الطريقة 1: تحميل من هنا (Make)

1. **اضغط على "Export"** في أعلى الصفحة
2. **اختر "Download as ZIP"**
3. **فك ضغط الملف** في مجلد على جهازك

### الطريقة 2: نسخ الملفات يدوياً

1. انسخ كل الملفات من القائمة على اليسار
2. ضعها في مجلد جديد على جهازك

---

## 🔧 الخطوات المطلوبة للتشغيل

### الخطوة 1️⃣: تثبيت Node.js

**هل Node.js مثبت عندك؟**
افتح Terminal (أو Command Prompt) واكتب:

```bash
node --version
```

**إذا ظهرت رسالة خطأ:**

1. روح على: https://nodejs.org/
2. حمل النسخة **LTS** (الموصى بها)
3. ثبتها زي أي برنامج عادي
4. بعد التثبيت، افتح Terminal جديد وجرب `node --version` تاني

---

### الخطوة 2️⃣: فتح Terminal في مجلد المشروع

**على Windows:**

1. افتح مجلد المشروع
2. اضغط على شريط العنوان (Address bar) في الأعلى
3. اكتب `cmd` واضغط Enter
4. هيفتح Terminal في نفس المكان

**على Mac/Linux:**

1. افتح Terminal
2. اكتب `cd` وبعدين مسافة
3. اسحب مجلد المشروع على Terminal
4. اضغط Enter

---

### الخطوة 3️⃣: تثبيت المكتبات

في Terminal، اكتب الأمر ده:

```bash
npm install
```

⏱️ **هيستغرق من 2-5 دقائق** (حسب سرعة النت)

**هتشوف:**

- كتير من الأسطر بتتحرك
- رسايل بتقول "installing..."
- في الآخر: "added XXX packages"

**إذا ظهرت أخطاء:**

- تأكد إنك متصل بالإنترنت
- جرب تشغل Terminal كـ Administrator/Root
- أو جرب: `npm install --legacy-peer-deps`

---

### الخطوة 4️⃣: تشغيل المشروع

بعد ما التثبيت يخلص، اكتب:

```bash
npm run dev
```

✅ **هتشوف رسالة زي كده:**

```
  VITE v5.x.x  ready in 300 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

---

### الخطوة 5️⃣: فتح الموقع

1. **افتح أي Browser** (Chrome, Firefox, Edge, Safari)
2. **اكتب في شريط العنوان:**
   ```
   http://localhost:5173
   ```
3. **اضغط Enter**

🎉 **الموقع هيفتح قدامك!**

---

## 🛑 كيفية إيقاف المشروع

في Terminal، اضغط:

- **Windows:** `Ctrl + C`
- **Mac:** `Cmd + C`

---

## 🔄 إعادة التشغيل

في أي وقت عايز تشغل المشروع تاني:

1. افتح Terminal في مجلد المشروع
2. اكتب: `npm run dev`
3. افتح http://localhost:5173

---

## ⚠️ مشاكل شائعة وحلولها

### مشكلة: `npm: command not found`

**الحل:** ثبت Node.js من الموقع الرسمي

---

### مشكلة: `Port 5173 is already in use`

**الحل:**

1. أغلق أي برنامج تاني شغال على نفس المنفذ
2. أو غير المنفذ بالأمر ده:

```bash
npm run dev -- --port 3000
```

---

### مشكلة: الصفحة بيضاء فاضية

**الحل:**

1. افتح Developer Console (اضغط F12)
2. شوف الأخطاء في Console
3. غالباً المشكلة من الـ API

---

### مشكلة: الخريطة مش ظاهرة

**الحل:**

1. تأكد من الاتصال بالإنترنت
2. الخريطة بتستخدم OpenStreetMap وتحتاج نت

---

## 📁 هيكل المشروع المهم

```
bloodlink/
├── App.tsx              ← الملف الرئيسي
├── components/          ← كل الـ Components
├── services/            ← API functions
├── hooks/               ← Custom React hooks
├── styles/              ← ملفات CSS
└── package.json         ← قائمة المكتبات
```

---

## 🔌 ربط المشروع بـ API

### 1. أنشئ ملف `.env` في المجلد الرئيسي

```env
VITE_API_BASE_URL=http://localhost:5000/api
```

### 2. غير الـ URL لعنوان الـ API الحقيقي بتاعك

```env
VITE_API_BASE_URL=https://your-api-url.com/api
```

### 3. شوف ملف `API_DOCUMENTATION.md` لمعرفة كل الـ Endpoints المطلوبة

---

## 📦 بناء المشروع للنشر (Production)

عشان تبني نسخة نهائية جاهزة للنشر:

```bash
npm run build
```

هيتم إنشاء مجلد `dist` فيه الملفات الجاهزة.

**للمزيد من التفاصيل عن النشر، شوف ملف `DEPLOYMENT.md`**

---

## 📞 محتاج مساعدة؟

1. **شوف ملف `README.md`** - فيه تفاصيل أكتر
2. **شوف ملف `API_DOCUMENTATION.md`** - لو المشكلة في API
3. **افتح Developer Console (F12)** - عشان تشوف الأخطاء

---

## ✅ Checklist قبل ما تبدأ

- [ ] Node.js مثبت (جرب `node --version`)
- [ ] فتحت Terminal في مجلد المشروع
- [ ] كتبت `npm install` ومستني التثبيت يخلص
- [ ] كتبت `npm run dev`
- [ ] فتحت http://localhost:5173 في المتصفح

---

## 🎯 أوامر مفيدة

```bash
# تشغيل المشروع
npm run dev

# بناء للإنتاج
npm run build

# معاينة النسخة النهائية
npm run preview

# تثبيت مكتبة جديدة
npm install package-name
```

---

🎉 **مبروك! المشروع شغال عندك دلوقتي**

لو عندك أي سؤال، ارجع للملفات:

- `README.md` - دليل شامل
- `API_DOCUMENTATION.md` - توثيق API
- `DEPLOYMENT.md` - دليل النشر

**Version:** 2.5.1
**Last Updated:** October 2025